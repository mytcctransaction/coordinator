#spring:
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        service-id: microservice-config-server
#      name: rabbitmq,redis
#      profile: ${cloud_profile:dev}

server:
  port: 9021
spring:
  application:
    name: consumer

